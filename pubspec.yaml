name: tirtc_av_kit_example
description: Two-page downlink demo for the tirtc_av_kit plugin.
publish_to: 'none'
version: 0.1.0-dev

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  crypto: ^3.0.6
  flutter:
    sdk: flutter
  mobile_scanner: 6.0.11
  tirtc_av_kit: 2.2.0
dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
